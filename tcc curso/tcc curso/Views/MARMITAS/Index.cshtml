@model IEnumerable<tcc_curso.Models.MARMITAS>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PRECO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.INGREDIENTES)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.QUANTIDADE)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PRECO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.INGREDIENTES)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.QUANTIDADE)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.COD_MARM }) |
            @Html.ActionLink("Details", "Details", new { id=item.COD_MARM }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.COD_MARM })
        </td>
    </tr>
}

</table>
